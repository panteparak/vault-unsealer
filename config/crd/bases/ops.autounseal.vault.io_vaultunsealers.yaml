---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: vaultunsealers.ops.autounseal.vault.io
spec:
  group: ops.autounseal.vault.io
  names:
    kind: VaultUnsealer
    listKind: VaultUnsealerList
    plural: vaultunsealers
    singular: vaultunsealer
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: VaultUnsealer is the Schema for the vaultunsealers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VaultUnsealerSpec defines the desired state of VaultUnsealer.
            properties:
              interval:
                type: string
              keyThreshold:
                type: integer
              mode:
                description: ModeSpec defines the unsealing strategy.
                properties:
                  ha:
                    type: boolean
                required:
                - ha
                type: object
              unsealKeysSecretRefs:
                items:
                  description: SecretRef is a reference to a key in a Kubernetes Secret.
                  properties:
                    key:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                  required:
                  - key
                  - name
                  type: object
                type: array
              vault:
                description: VaultConnectionSpec defines how to connect to the Vault
                  cluster.
                properties:
                  caBundleSecretRef:
                    description: SecretRef is a reference to a key in a Kubernetes
                      Secret.
                    properties:
                      key:
                        type: string
                      name:
                        type: string
                      namespace:
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  insecureSkipVerify:
                    type: boolean
                  url:
                    type: string
                required:
                - url
                type: object
              vaultLabelSelector:
                type: string
            required:
            - mode
            - unsealKeysSecretRefs
            - vault
            - vaultLabelSelector
            type: object
          status:
            description: VaultUnsealerStatus defines the observed state of VaultUnsealer.
            properties:
              conditions:
                items:
                  description: Condition represents the state of a resource.
                  properties:
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              lastReconcileTime:
                format: date-time
                type: string
              podsChecked:
                items:
                  type: string
                type: array
              unsealedPods:
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
